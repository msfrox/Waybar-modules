#!/usr/bin/env python3
"""Entry point for the Control Center settings app.

Standalone by design — see wccsettings/window.py. Launch it from the Control
Center's own quick action, from the .desktop entry, or directly.
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from wccsettings.window import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main(sys.argv))
